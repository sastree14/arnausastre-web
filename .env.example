# Website contact form
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
CONTACT_TO_EMAIL=arnau.sastre@sc-analytics.io

# Growth Agent — LLM and research
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-2
BRAVE_API_KEY=
APOLLO_API_KEY=

# Growth Agent — persistent state and private generated assets
SUPABASE_URL=
SUPABASE_SECRET_KEY=
# Optional legacy fallback only; new installs should use SUPABASE_SECRET_KEY.
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ASSET_BUCKET=growth-assets
STATE_PROVIDER=local
ASSET_PROVIDER=local

# Growth Agent — private approval console on /growth-admin
GROWTH_ADMIN_TOKEN=
# Exactly 32 random bytes encoded as base64. Never expose this client-side.
GROWTH_ENCRYPTION_KEY=

# LinkedIn OAuth — personal Arnau account
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_REDIRECT_URI=https://sc-analytics.io/api/linkedin/callback
LINKEDIN_API_VERSION=202608

# Legacy LinkedIn static credentials. Not needed once OAuth is connected.
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_ORGANIZATION_ID=
LINKEDIN_PERSON_URN=

# Legacy editorial system (v1)
GOOGLE_SHEETS_CREDENTIALS=
EDITORIAL_SHEET_ID=
GEMINI_API_KEY=
