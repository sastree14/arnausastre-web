import { randomUUID } from 'node:crypto'
import { NextResponse } from 'next/server'
import { insertGrowthRow, isGrowthAdminAuthenticated } from '@/lib/growth-admin'
import { queryGrowthTable } from '@/lib/supabase-growth'
import { dispatchOperatorQueue } from '@/lib/github-actions'
const ACTION_TO_TYPE:Record<string,string>={editorial_proposals:'OPERATOR_EDITORIAL_PROPOSALS',editorial_run:'OPERATOR_EDITORIAL_RUN',editorial_url:'OPERATOR_EDITORIAL_URL',rewrite_content:'OPERATOR_REWRITE_CONTENT',prospect:'OPERATOR_PROSPECT',commercial_signals:'OPERATOR_COMMERCIAL_SIGNALS',competitor_discover:'OPERATOR_COMPETITOR_DISCOVER',competitor_refresh:'OPERATOR_COMPETITOR_REFRESH',competitor_refresh_all:'OPERATOR_COMPETITOR_REFRESH_ALL',publish_linkedin:'OPERATOR_PUBLISH_LINKEDIN',publish_article:'OPERATOR_PUBLISH_ARTICLE',unpublish_linkedin:'OPERATOR_UNPUBLISH_LINKEDIN',unpublish_article:'OPERATOR_UNPUBLISH_ARTICLE',deal_proposal:'OPERATOR_DEAL_PROPOSAL',deal_budget:'OPERATOR_DEAL_BUDGET',gmail_sync:'OPERATOR_GMAIL_SYNC',seo_audit:'OPERATOR_SEO_AUDIT'}
type TaskRow={task_id:string;type:string;status:string;inputs?:Record<string,unknown>|null;created_at?:string|null}
const int=(value:FormDataEntryValue|null,fallback:number,max=100)=>{const parsed=Number(value);return Number.isFinite(parsed)?Math.min(max,Math.max(1,Math.floor(parsed))):fallback};const bool=(value:FormDataEntryValue|null)=>['1','true','yes','on'].includes(String(value||'').toLowerCase());const sameInputs=(a:Record<string,unknown>|null|undefined,b:Record<string,unknown>)=>JSON.stringify(Object.entries(a||{}).sort())===JSON.stringify(Object.entries(b).sort())
export async function POST(request:Request){if(!(await isGrowthAdminAuthenticated()))return new NextResponse('Unauthorized',{status:401});const form=await request.formData(),action=String(form.get('action')||''),type=ACTION_TO_TYPE[action];if(!type)return new NextResponse('Unsupported operator action',{status:400});let inputs:Record<string,unknown>={};if(action==='editorial_proposals')inputs={focus:String(form.get('focus')||'').trim(),avoid:String(form.get('avoid')||'').trim(),count:int(form.get('count'),3,6),history_days:int(form.get('history_days'),60,180)};else if(action==='editorial_run')inputs={theme_hint:String(form.get('theme_hint')||'').trim(),avoid:String(form.get('avoid')||'').trim(),max_signals:int(form.get('max_signals'),30),max_briefs:int(form.get('max_briefs'),1),strict_theme:bool(form.get('strict_theme')),force_new:bool(form.get('force_new')),source_proposal_key:String(form.get('source_proposal_key')||'').trim()};else if(action==='editorial_url'){const url=String(form.get('url')||'').trim();if(!url)return new NextResponse('Missing URL',{status:400});inputs={url,title:String(form.get('title')||'').trim()}}else if(['rewrite_content','publish_linkedin','publish_article','unpublish_linkedin','unpublish_article'].includes(action)){const contentId=String(form.get('content_id')||'').trim();if(!contentId)return new NextResponse('Missing content_id',{status:400});inputs={content_id:contentId}}else if(action==='prospect')inputs={mode:String(form.get('mode')||'lead'),limit:int(form.get('limit'),10,50)};else if(action==='commercial_signals')inputs={limit:int(form.get('limit'),12,40)};else if(action==='competitor_discover')inputs={limit:int(form.get('limit'),8,20),focus:String(form.get('focus')||'').trim()};else if(action==='competitor_refresh'){const competitorId=String(form.get('competitor_id')||'').trim();if(!competitorId)return new NextResponse('Missing competitor_id',{status:400});inputs={competitor_id:competitorId,max_events:int(form.get('max_events'),8,12)}}else if(action==='competitor_refresh_all')inputs={limit:int(form.get('limit'),20,50)};else if(action==='deal_proposal'||action==='deal_budget'){const opportunityId=String(form.get('opportunity_id')||'').trim();if(!opportunityId)return new NextResponse('Missing opportunity_id',{status:400});inputs={opportunity_id:opportunityId}}else if(action==='gmail_sync')inputs={limit:int(form.get('limit'),50,100),newer_than_days:int(form.get('newer_than_days'),14,30)};else if(action==='seo_audit')inputs={limit:int(form.get('limit'),6,12)};const recent=await queryGrowthTable<TaskRow>('tasks',{tenant_id:'eq.sc-analytics',type:`eq.${type}`,order:'created_at.desc',limit:'5'},{cacheSeconds:0}),duplicate=recent.find(task=>['queued','pending','running'].includes(String(task.status||''))&&Number.isFinite(Date.parse(String(task.created_at||'')))&&Date.now()-Date.parse(String(task.created_at))<=30000&&sameInputs(task.inputs,inputs)),taskId=duplicate?.task_id||`task_${randomUUID().replaceAll('-','').slice(0,12)}`;if(!duplicate){const now=new Date().toISOString();await insertGrowthRow('tasks',{task_id:taskId,tenant_id:'sc-analytics',type,scheduled_for:now,status:'queued',requires_approval:false,inputs,outputs:{},created_at:now})}const dispatch=await dispatchOperatorQueue(taskId),returnTo=String(form.get('return_to')||'/growth-admin'),url=new URL(returnTo.startsWith('/')?returnTo:'/growth-admin',request.url);url.searchParams.set('queued',taskId);url.searchParams.set('dispatched',dispatch.dispatched?'1':'0');url.searchParams.set('deduplicated',duplicate?'1':'0');if(!dispatch.dispatched)url.searchParams.set('dispatch_reason',dispatch.reason);return NextResponse.redirect(url,303)}
