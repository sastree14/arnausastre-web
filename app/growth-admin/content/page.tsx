import {redirect} from 'next/navigation'
import AdminShell from '@/components/growth-admin/AdminShell'
import EditorialPlanner from '@/components/growth-admin/EditorialPlanner'
import ConfirmFormButton from '@/components/growth-admin/ConfirmFormButton'
import {Badge,EmptyState,PageHeader,SectionHeading,adminButtonPrimary,adminButtonSecondary,adminInput,adminPanel,assetUrl,formatDate,publicationLabel,scheduleInputValue,statusTone} from '@/components/growth-admin/AdminUi'
import {evaluatePublicationReadiness} from '@/lib/growth-approval'
import {getPendingApprovals,isGrowthAdminAuthenticated,type GrowthEditorialBrief,type GrowthContentItem,type GrowthTask} from '@/lib/growth-admin'
import {getEditorialWorkspaceResetAt,getWorkspaceContent,getWorkspaceEditorialBriefs,getWorkspaceEditorialTasks} from '@/lib/editorial-workspace'
export const dynamic='force-dynamic';export const revalidate=0
type Stage='review'|'visual'|'ready'|'scheduled'|'published'
const textParam=(value:string|string[]|undefined,fallback='')=>typeof value==='string'?value:fallback
function criticWarnings(critique?:Record<string,unknown>|null){if(!critique)return[];const rows:string[]=[];if(critique.rewrite_required===true)rows.push('La revisión automática recomienda reescritura');if(critique.contract_valid===false)rows.push('La revisión automática detecta un problema de contrato editorial');const risk=Number(critique.generic_ai_risk||0);if(risk>=6)rows.push(`Riesgo de estilo genérico ${risk.toFixed(1)}/10`);return rows}
function stageFor(item:GrowthContentItem,readiness:ReturnType<typeof evaluatePublicationReadiness>):Stage{if(item.status==='published')return'published';if(item.status==='scheduled'||Boolean(item.scheduled_at))return'scheduled';if(item.status==='approved')return'ready';if(readiness.visualRequired&&!readiness.hasVisual)return'visual';return'review'}
const gateTone=(ok:boolean)=>ok?'border-emerald-200 bg-emerald-50 text-emerald-700':'border-amber-200 bg-amber-50 text-amber-700'
export default async function ContentPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){if(!(await isGrowthAdminAuthenticated()))redirect('/growth-admin/login');const params=await searchParams;const[content,briefs,approvals,tasks,resetAt]=await Promise.all([getWorkspaceContent(),getWorkspaceEditorialBriefs(),getPendingApprovals(),getWorkspaceEditorialTasks(),getEditorialWorkspaceResetAt()]);const briefById=new Map(briefs.map(row=>[row.brief_id,row])),approvalByTarget=new Map(approvals.filter(row=>['publish_post','publish_article'].includes(row.action_type)).map(row=>[row.target_id,row])),filter=textParam(params.filter,'review'),q=textParam(params.q).trim().toLowerCase(),show=textParam(params.show),queued=textParam(params.queued),currentTask=queued?tasks.find(task=>task.task_id===queued):undefined,proposalTasks=tasks.filter(task=>task.type==='OPERATOR_EDITORIAL_PROPOSALS'&&task.status==='completed'&&Array.isArray(task.outputs?.proposals));const rewriteRunning=new Set(tasks.filter(task=>task.type==='OPERATOR_REWRITE_CONTENT'&&['queued','pending','running'].includes(task.status)).map(task=>String(task.inputs?.content_id||'')))
  const rows=content.filter(item=>!['rejected','failed','superseded_test','alternate'].includes(item.status)).map(item=>{const brief=item.brief_id?briefById.get(item.brief_id) as GrowthEditorialBrief|undefined:undefined,readiness=evaluatePublicationReadiness(item,brief),warnings=[...new Set([...criticWarnings(item.critique),...readiness.issues])],stage=stageFor(item,readiness);return{item,brief,readiness,warnings,stage,approval:approvalByTarget.get(item.content_id)}})
  const counts={review:rows.filter(r=>r.stage==='review').length,visual:rows.filter(r=>r.stage==='visual').length,ready:rows.filter(r=>r.stage==='ready').length,scheduled:rows.filter(r=>r.stage==='scheduled').length,published:rows.filter(r=>r.stage==='published').length}
  const filtered=rows.filter(row=>(filter==='all'||row.stage===filter)&&(!q||`${row.item.title} ${row.item.body||''}`.toLowerCase().includes(q))),visible=q||show==='all'?filtered:filtered.slice(0,3)
  return <AdminShell active="content"><PageHeader eyebrow="CRM · Editorial" title="Editorial" description="Propuestas, copy, visual, decisión humana, calendario y publicación en un único funnel. Cada pieza aparece en un solo estado." actions={<><form action="/api/growth-admin/operator-task" method="post"><input type="hidden" name="action" value="editorial_run"/><input type="hidden" name="max_briefs" value="1"/><input type="hidden" name="max_signals" value="30"/><input type="hidden" name="force_new" value="1"/><input type="hidden" name="return_to" value="/growth-admin/content"/><button className="rounded-lg bg-white px-4 py-2.5 text-sm font-semibold text-slate-950">Generación directa</button></form><a href="/growth-admin/visual-studio" className="rounded-lg border border-slate-700 px-4 py-2.5 text-sm font-semibold text-white">Visual Studio</a><a href="/growth-admin/calendar" className="rounded-lg border border-slate-700 px-4 py-2.5 text-sm font-semibold text-white">Calendario</a></>}/>
    {resetAt&&<div className="mb-4 rounded-xl border border-slate-200 bg-white px-4 py-2 text-xs text-slate-500">Workspace activo desde <strong>{formatDate(resetAt,true)}</strong>.</div>}{params.blocked&&<div className="mb-4 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800">La revisión automática ha detectado alertas. Puedes corregirlas o aprobar manualmente igualmente.</div>}{(params.approved||params.edited||params.deleted)&&<div className="mb-4 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-800">Acción guardada correctamente.</div>}
    <EditorialPlanner content={content} proposalTasks={proposalTasks} currentTask={currentTask}/>
    <section className="mb-5 grid grid-cols-2 gap-2 md:grid-cols-5">{([['review','Revisión',counts.review],['visual','Visual',counts.visual],['ready','Listas',counts.ready],['scheduled','Programadas',counts.scheduled],['published','Publicadas',counts.published]] as const).map(([key,label,count])=><a key={key} href={`/growth-admin/content?filter=${key}`} className={`${adminPanel} px-4 py-3 ${filter===key?'border-indigo-300 bg-indigo-50':''}`}><p className="text-[10px] font-semibold uppercase tracking-wide text-slate-400">{label}</p><p className="mt-1 text-2xl font-semibold">{count}</p></a>)}</section>
    <div className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><form method="get" className="flex min-w-0 flex-1 gap-2"><input type="hidden" name="filter" value={filter}/><input name="q" defaultValue={textParam(params.q)} placeholder="Buscar por título o texto" className={`w-full max-w-lg ${adminInput}`}/><button className={adminButtonSecondary}>Buscar</button></form><a href="/growth-admin/content?filter=all" className="text-xs font-semibold text-slate-500">Ver todas las fases</a></div>
    <SectionHeading eyebrow="Editorial workflow" title={filter==='all'?'Todas las publicaciones':filter==='visual'?'Visual pendiente':filter==='ready'?'Listas para distribuir':filter==='scheduled'?'Programadas':filter==='published'?'Publicadas':'Por revisar'} description="Se muestran tres por defecto para mantener la pantalla operativa. Abre únicamente la pieza que quieras trabajar." count={filtered.length}/>
    <div className="space-y-3">{!visible.length&&<EmptyState>No hay piezas en este estado.</EmptyState>}{visible.map(({item,readiness,warnings,stage,approval})=>{const image=assetUrl(item),approved=['ready','scheduled','published'].includes(stage),rewriting=rewriteRunning.has(item.content_id),reviewOk=warnings.length===0,visualOk=!readiness.visualRequired||readiness.hasVisual;return <details key={item.content_id} className={`${adminPanel} group overflow-hidden`}><summary className="cursor-pointer list-none px-5 py-4"><div className="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div className="min-w-0"><div className="flex flex-wrap gap-2"><Badge tone={statusTone(item.status)}>{stage}</Badge><Badge tone={item.content_type==='article'?'violet':'blue'}>{publicationLabel(item)}</Badge><Badge>{(item.language||'—').toUpperCase()}</Badge></div><h2 className="mt-2 truncate text-base font-semibold">{item.title}</h2></div><div className="flex flex-wrap gap-2"><span className={`rounded-full border px-2 py-1 text-[10px] font-semibold ${gateTone(reviewOk)}`}>{reviewOk?'Revisión automática OK':'Alertas automáticas'}</span><span className={`rounded-full border px-2 py-1 text-[10px] font-semibold ${gateTone(visualOk)}`}>{visualOk?'Visual OK':'Visual pendiente'}</span><span className={`rounded-full border px-2 py-1 text-[10px] font-semibold ${gateTone(approved)}`}>{approved?'Aprobada':'Decisión pendiente'}</span><span className="px-2 text-slate-400">⌄</span></div></div></summary><div className="border-t border-slate-200 bg-slate-50/50 p-5"><div className="grid gap-5 xl:grid-cols-[160px_minmax(0,1fr)_340px]"><div>{image?<img src={image} alt={item.title} className="h-28 w-full rounded-xl border border-slate-200 bg-white object-contain"/>:<div className="flex h-28 items-center justify-center rounded-xl border border-dashed border-slate-300 bg-white text-xs text-slate-400">Sin visual</div>}<a href={`/growth-admin/visual-studio?content=${encodeURIComponent(item.content_id)}`} className="mt-2 block rounded-lg border border-indigo-200 bg-white px-3 py-2 text-center text-xs font-semibold text-indigo-700">{image?'Editar visual':'Diseñar visual'}</a></div><div><p className="line-clamp-5 whitespace-pre-wrap text-sm leading-6 text-slate-600">{item.body}</p>{warnings.length>0&&<div className="mt-3 rounded-xl border border-amber-200 bg-amber-50 p-3"><p className="text-xs font-semibold text-amber-900">Revisión automática (orientativa)</p><p className="mt-1 text-[11px] text-amber-800">No bloquea tu decisión. Puedes editar, pedir otra versión o aprobar manualmente.</p><ul className="mt-2 list-disc space-y-1 pl-4 text-[10px] text-amber-800">{warnings.map(w=><li key={w}>{w}</li>)}</ul></div>}<details className="mt-3 rounded-xl border border-slate-200 bg-white p-3"><summary className="cursor-pointer text-xs font-semibold text-slate-700">Editar texto manualmente</summary><form action="/api/growth-admin/content-edit" method="post" className="mt-3"><input type="hidden" name="content_id" value={item.content_id}/><input type="hidden" name="return_to" value={`/growth-admin/content?filter=${stage}`}/><input name="title" defaultValue={item.title} className={`w-full ${adminInput}`}/><textarea name="body" defaultValue={item.body||''} className={`mt-2 min-h-48 w-full ${adminInput}`}/><button className={`mt-2 ${adminButtonSecondary}`}>Guardar cambios</button></form></details><div className="mt-3 flex flex-wrap gap-2"><a href={`/growth-admin/preview/${item.content_id}`} className={adminButtonSecondary}>Preview final</a>{!approved&&<form action="/api/growth-admin/operator-task" method="post"><input type="hidden" name="action" value="rewrite_content"/><input type="hidden" name="content_id" value={item.content_id}/><input type="hidden" name="return_to" value="/growth-admin/content?filter=review"/><button disabled={rewriting} className="rounded-lg border border-indigo-200 bg-indigo-50 px-4 py-2.5 text-sm font-semibold text-indigo-700 disabled:opacity-50">{rewriting?'Generando otra versión…':'Proponer nueva versión con IA'}</button></form>}</div></div><div className="space-y-3">{!approved&&<form action="/api/growth-admin/decide" method="post" className="rounded-xl border border-indigo-200 bg-indigo-50 p-4"><input type="hidden" name="content_id" value={item.content_id}/>{approval&&<input type="hidden" name="approval_id" value={approval.approval_id}/>}<input type="hidden" name="decision" value="approved"/><input type="hidden" name="manual_override" value="1"/><input type="hidden" name="return_to" value="/growth-admin/content?filter=ready"/><p className="text-xs font-semibold text-indigo-900">Decisión humana</p><p className="mt-1 text-[11px] leading-5 text-indigo-700">Puedes aprobar aunque existan alertas automáticas. La decisión queda registrada.</p><button className={`mt-3 w-full ${adminButtonPrimary}`}>Aprobar manualmente</button></form>}{(stage==='ready'||stage==='scheduled')&&<form action="/api/growth-admin/schedule" method="post" className="rounded-xl border border-slate-200 bg-white p-4"><input type="hidden" name="content_id" value={item.content_id}/><label className="text-[11px] font-medium text-slate-500">Añadir al calendario<input name="scheduled_at" type="datetime-local" defaultValue={scheduleInputValue(item.scheduled_at)} className={`mt-2 w-full ${adminInput}`}/></label><div className="mt-2 flex flex-wrap gap-2"><button className={adminButtonSecondary}>Guardar fecha</button>{item.status==='approved'&&<button formAction="/api/growth-admin/publish-now" className={adminButtonPrimary}>Publicar ahora</button>}</div></form>}<ConfirmFormButton action="/api/growth-admin/content-delete" fields={{content_id:item.content_id,return_to:`/growth-admin/content?filter=${stage}`}} label="Eliminar publicación" message="¿Eliminar esta publicación del CRM?"/></div></div></div></details>})}</div>
    {!q&&filtered.length>3&&<div className="mt-4"><a href={`/growth-admin/content?filter=${encodeURIComponent(filter)}&show=${show==='all'?'3':'all'}`} className="text-xs font-semibold text-indigo-700">{show==='all'?'Mostrar solo 3':`Ver ${filtered.length-3} más`}</a></div>}
  </AdminShell>}
