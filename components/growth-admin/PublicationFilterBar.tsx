'use client'
import {FormEvent}from'react'
import {usePathname,useRouter,useSearchParams}from'next/navigation'

type Props={publication:string;review:string;approval:string;query:string;total:number}
const selectClass='rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700'
const FLOW=['1 · Borrador','2 · Revisión','3 · Aprobada','4 · Programada','5 · Publicada']
export default function PublicationFilterBar({publication,review,approval,query,total}:Props){const router=useRouter(),pathname=usePathname(),searchParams=useSearchParams();function update(name:string,value:string){const next=new URLSearchParams(searchParams.toString());if(value==='all'||!value)next.delete(name);else next.set(name,value);next.delete('show');router.replace(`${pathname}?${next.toString()}`,{scroll:false})}function search(event:FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget),next=new URLSearchParams(searchParams.toString()),value=String(data.get('q')||'').trim();if(value)next.set('q',value);else next.delete('q');next.delete('show');router.replace(`${pathname}?${next.toString()}`,{scroll:false})}return <div className="mb-5 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="mb-4 flex flex-wrap items-center gap-1.5 border-b border-slate-100 pb-4"><span className="mr-1 text-[10px] font-semibold uppercase tracking-wide text-slate-400">Orden del flujo</span>{FLOW.map((step,index)=><span key={step} className="flex items-center gap-1.5"><span className="rounded-full border border-slate-200 bg-slate-50 px-2.5 py-1 text-[10px] font-semibold text-slate-600">{step}</span>{index<FLOW.length-1&&<span className="text-xs text-slate-300">→</span>}</span>)}</div><div className="flex flex-col gap-3 xl:flex-row xl:items-end xl:justify-between"><div className="grid gap-3 sm:grid-cols-3"><label className="text-[10px] font-semibold uppercase tracking-wide text-slate-400">Publicación<select value={publication} onChange={e=>update('publication',e.target.value)} className={`mt-1 block w-full ${selectClass}`}><option value="unpublished">No publicada</option><option value="delivered">Programada o publicada</option><option value="all">Todas</option></select></label><label className="text-[10px] font-semibold uppercase tracking-wide text-slate-400">Revisión<select value={review} onChange={e=>update('review',e.target.value)} className={`mt-1 block w-full ${selectClass}`}><option value="all">Todas</option><option value="pending">Por revisar</option><option value="reviewed">Revisadas</option></select></label><label className="text-[10px] font-semibold uppercase tracking-wide text-slate-400">Aprobación<select value={approval} onChange={e=>update('approval',e.target.value)} className={`mt-1 block w-full ${selectClass}`}><option value="all">Todas</option><option value="pending">No aprobadas</option><option value="approved">Aprobadas</option></select></label></div><form onSubmit={search} className="flex min-w-0 flex-1 gap-2 xl:max-w-xl"><input name="q" defaultValue={query} placeholder="Buscar publicación por título o texto" className="w-full rounded-lg border border-slate-200 px-3 py-2 text-sm"/><button className="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-700">Buscar</button></form></div><div className="mt-3 flex items-center justify-between border-t border-slate-100 pt-3"><p className="text-xs text-slate-500"><strong className="text-slate-800">{total}</strong> publicaciones coinciden con los filtros.</p><button type="button" onClick={()=>router.replace(pathname,{scroll:false})} className="text-xs font-semibold text-indigo-700">Limpiar filtros</button></div></div>}
